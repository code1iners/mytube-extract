{
  "name": "@mytube-extract/media-downloader",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "node --test dist/*.spec.js",
    "test:cov": "node --experimental-test-coverage --test-coverage-include=dist/**/*.js --test-coverage-exclude=dist/**/*.spec.js --test dist/*.spec.js"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "typescript": "^5.1.3"
  }
}
