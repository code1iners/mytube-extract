DATABASE_URL=
DIRECT_URL=

R2_ENDPOINT=
R2_BUCKET=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

ASSET_RETENTION_DAYS=7
WORKER_LOOP_INTERVAL_MS=5000
WORKER_PROCESSING_TIMEOUT_MS=3600000
SUBTITLE_AUDIO_MAX_BYTES=536870912
# ⚠️ WARNING: 세 값 모두 worker 기동 preflight 대상이다. 하나라도 비어 있거나 파일이 없으면 worker 전체(다운로드 job 포함)가 기동을 거부한다.
# whisper.cpp를 git clone만 해서는 실제 모델이 없다 — models/download-ggml-model.sh base.en, models/download-ggml-model.sh small.en으로 받아야 한다.
WHISPER_CLI_PATH=
WHISPER_MODEL_BASE_EN_PATH=
WHISPER_MODEL_SMALL_EN_PATH=
WHISPER_THREADS=4
