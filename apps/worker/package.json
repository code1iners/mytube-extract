{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "description": "Local MyTube Extract FIFO worker.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --env-file-if-exists=.env -r ts-node/register src/main.ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tsc --noEmit",
    "start": "node --env-file-if-exists=.env dist/main.js",
    "test": "node --test dist/*.spec.js",
    "test:cov": "node --experimental-test-coverage --test-coverage-include=dist/**/*.js --test-coverage-exclude=dist/**/*.spec.js --test dist/*.spec.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1077.0",
    "@aws-sdk/lib-storage": "^3.1077.0",
    "@mytube-extract/db": "workspace:*",
    "@mytube-extract/media-downloader": "workspace:*",
    "youtube-dl-exec": "3.1.8"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
