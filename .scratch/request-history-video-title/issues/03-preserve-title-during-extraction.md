# 03: 신규 추출의 제목을 확보 즉시 표시

**What to build:** 신규 영상·오디오 추출에서 제목을 얻으면 완료 전에 요청 내역에 표시하고, 이후 추출이 실패해도 같은 제목을 유지한다. 제목을 얻지 못하더라도 기존 추출을 계속하고 원본 링크로 요청을 식별한다.

**Blocked by:** 01 작업 처리 진입점 분리, 02 재사용 결과물의 제목 보존과 내역 표시.

Status: ready-for-human

- [x] 기존 제목 조회에서 유효한 제목을 얻으면 다운로드·업로드 완료 전에 요청에 저장한다. 제목 확보를 위해 기존 사전 검증과 추출 순서를 바꾸거나 접수 시 별도 조회를 추가하지 않는다.
- [x] 조건부 저장으로 처음 확보한 유효한 제목만 보존한다. 중복 처리·동시 저장 시도·다른 제목을 반환하는 재실행에서도 이미 저장한 제목을 덮어쓰지 않는다.
- [x] 제목 조회 실패나 공백뿐인 결과는 미확보로 처리하며 다운로드를 계속한다. 저장 실패를 제목 보존 성공으로 보고하지 않고 기존 오류 처리와 구분한다.
- [x] 제목 확보 전 실패한 요청은 원본 링크를 제공하고, 확보 후 다운로드 또는 업로드가 실패한 요청은 제목을 유지한다.
- [x] 기존 상태 조회 주기에서 링크가 제목으로 바뀐다. 제목 전용 반복 조회, 종료 상태의 반복 조회, 제목 변경에 대한 반복 공지를 추가하지 않는다.
- [ ] 01의 실제 처리 진입점과 02의 로컬 데이터베이스 검증 경계를 사용한다. 제목 확보 직후 다운로드를 일시 정지하고 실제 서버 상태 조회에서 완료 전 제목을 확인한다.
- [ ] 같은 요청을 실패로 종료한 뒤 재조회하여 제목 보존을 확인한다. 제목 조회 실패 후 추출 성공과 제목 확보 전 실패도 검증한다.
- [ ] 실제 로컬 데이터베이스에서 경쟁하는 제목 저장 시도와 재실행을 검증하여 첫 저장값이 유지됨을 확인한다. 대역 기반 처리 검증을 실제 외부 추출 성공으로 표현하지 않는다.
- [x] 기존 브라우저 검증에서 처리 중 링크 → 제목 전환과 실패 후 제목 표시를 확인하고, 마지막 변경 이후 관련 정적 검사·최신 작업 처리기 빌드·테스트를 통과한다.

## 범위 경계

과거 제목 재조회, 새로운 재시도 정책, 최신 YouTube 제목 동기화는 포함하지 않는다. 광범위한 작업 처리 구조 변경은 01의 경계를 재사용하여 피한다.

## Comments

- 2026-09-09: `processDownloadJob`의 기존 제목 조회 직후 요청 제목을 조건부 저장하도록 연결했다. 제목 조회 결과는 trim·유효성 정규화 후 저장하며, 조회 실패·공백 결과는 추출을 계속하고 제목 저장 오류는 기존 worker 실패 경계로 전달한다. 초기 제목이 비어 있는 동시 저장과 재실행에서 첫 저장값을 보존하는 실제 DB 저장 함수를 추가했다.
- 2026-09-09: worker 처리기 테스트 20개, API unit 204개, Web unit 154개, root `pnpm test` 12개 task, root `pnpm lint`, worker 최신 build, Web build 및 Chromium history smoke를 통과했다. smoke에서 처리 중 원본 링크가 확보 제목으로 바뀌고 실패 뒤 제목·재시도 동작이 유지됨을 확인했다. 대역 기반 worker·Chromium 결과는 실제 YouTube/R2 성공 증거가 아니다.
- 2026-09-09: 실제 PostgreSQL 스위트 6개를 `apps/api/.env`의 설정 DB로 실행했으나 `The column title does not exist in the current database`로 모두 실패했다. 저장소의 `20260909000000_add_extraction_job_title` migration은 외부 DB에 적용하지 않았으며, 따라서 실제 DB 저장·상태 조회·경쟁 저장 검증은 사람 확인 대기로 남긴다.
