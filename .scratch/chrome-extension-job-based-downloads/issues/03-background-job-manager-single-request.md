# 03: Background job 관리 모듈 — 팝업 단일 요청을 job 기반으로 전환

**What to build:** 팝업에서 추출을 요청하면 실제로 서버에 job이 생성되고, 처리 상태를 확인하다가 완료되면 자동으로 로컬 다운로드가 시작된다. (오버레이는 아직 대상 아님, 팝업은 아직 가장 최근 요청 하나만 보여준다.)

**Blocked by:** 01, 02

**Status:** done (구현 88297de)

- [x] 팝업에서 제출하면 직접 다운로드 API 대신 job이 생성된다.
- [x] 대기/처리 중 상태가 기존 처리 화면에 반영된다.
- [x] job이 완료되면 사용자 조작 없이 로컬 다운로드가 자동으로 시작된다.
- [x] job이 실패하면 기존 오류 화면에 실패 사유가 표시된다.
- [x] job 생성 직후에는 짧은 간격으로 상태를 빠르게 확인한다.
- [x] 내부적으로 여러 job을 동시에 추적할 수 있는 구조로 만들되(추후 목록 UI를 위한 선행 작업), 이 티켓에서 팝업 화면은 가장 최근 job 하나만 보여준다.
- [x] job 생성/폴링/완료 처리가 실제 Chrome API·네트워크 없이 가짜 의존성으로 검증된다.
