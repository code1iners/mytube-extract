# 13: 빈 요청 내역에서 두 추출 흐름과 보관 모델 안내

**What to build:** 요청 내역이 비어 있을 때 사용자가 영상·오디오 추출과 영어 자막 생성 중 원하는 작업을 바로 시작할 수 있게 한다. 동시에 이력은 현재 브라우저에만 저장되고 완료 파일은 7일 동안 보관된다는 MyTube Extract의 작업 모델을 행동 맥락 안에서 이해하게 한다.

**Blocked by:** 12: 주요 내비게이션을 영상·자막·내역으로 통합

**Status:** done (2026-09-01)

- [x] 빈 요청 내역에서 YouTube URL 기반 영상·오디오 추출과 로컬 영상 기반 영어 자막 생성이 동등한 두 진입점으로 제공된다.
- [x] 각 진입점의 출발 입력과 받을 결과가 기술 지식 없이도 구분된다.
- [x] 이력은 현재 브라우저에만 저장되고 완료 파일은 7일 동안 보관된다는 안내가 짧고 명확하게 표시된다.
- [x] 두 진입점은 12에서 확정한 주요 목적지와 같은 route·용어를 사용하며 키보드와 터치로 이동할 수 있다.
- [x] 기존 요청이 하나 이상 있을 때의 최신순 목록, 상태 조회, 다운로드, 다시 요청, 내역 삭제 동작은 변경되지 않는다.
- [x] 빈 상태의 두 진입점과 운영 안내를 검증하는 단위 테스트 및 모바일·데스크톱 브라우저 검증이 추가된다.

## Comments

- 빈 `/history` 상태를 `영상 추출`과 `자막 추출` 두 동등한 링크 카드로 확장했다. 각 카드에 YouTube URL 또는 로컬 영상이라는 입력과 영상·오디오 또는 영어 SRT라는 결과를 함께 표시하고, 브라우저 한정 이력·완료 파일 7일 보관 안내를 추가했다.
- 기존 요청이 있는 경우의 최신순 목록, 상태 조회, 다운로드, 재요청, 삭제 분기는 그대로 유지했다. 모바일에서는 두 진입점을 1열로 쌓고 데스크톱에서는 2열로 배치했다.
- 검증: `pnpm --filter web run lint`, `pnpm --filter web run test` (15 files, 93 tests), `pnpm --filter web run build`, `pnpm --filter web run test:browser` 통과. 빈 상태 브라우저 검증은 390px·1280px viewport에서 두 route link, 입출력 설명, 보관 안내, 반응형 열 배치를 확인한다.
